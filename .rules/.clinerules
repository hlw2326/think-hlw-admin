# HlwAdmin VSCode Cline & AI 辅助开发与编码规范指南

本文件是为 VSCode 的 Cline/Roo Code 插件等 AI 编码助手量身定制的完整开发规范指南。旨在规范前后端技术栈实践、代码风格、接口设计以及目录结构，确保 AI 生成的代码能直接无缝融入本项目中。

---

## 📂 项目全景与核心架构

本项目是一个基于前后端分离的**全栈管理系统与微信小程序**工程。

```text
hlw-admin/ (项目根目录)
├── php/             # 后端项目 (基于 ThinkPHP 8.2 + ThinkLibrary 6.1)
└── uni/             # 前端项目 (基于 Vue 3 + uni-app + Vite)
```

---

## 🛠️ 后端开发规范 (`/php`)

### 1. 技术栈与运行环境
*   **核心框架**：ThinkPHP 8.2 + ThinkLibrary 6.1
*   **PHP 版本**：`>= 8.2` (优先使用 PHP 8.2+ 强类型和新特性)
*   **包管理工具**：Composer
*   **数据库**：MySQL 5.7+ 或 SQLite (使用 SQLite 时注意定期备份 `database/sqlite.db`)
*   **运行目录**：Nginx/Apache 的站点运行目录必须指向 `public/`

### 2. 核心目录结构与分工
AI 在新增、修改后端代码时，请严格遵守以下目录职责：
*   `app/admin/`：后台系统管理模块（权限控制、系统配置等）。
*   `app/wechat/`：微信开放平台/小程序接口及微信相关逻辑处理模块。
*   `app/index/`：入口页面与通用公开模块。
*   `config/`：应用配置文件，新增配置应添加对应的扩展文件或写在现有配置文件中。
*   `database/`：数据库迁移脚本及本地 SQLite 数据库。
*   `package/`：本地插件目录。

> [!IMPORTANT]
> **绝对禁止提交以下文件与目录到 Git：**
> *   `/php/.env`（本地环境变量）
> *   `/php/runtime/`（运行时缓存与日志）
> *   `/php/vendor/`（Composer 依赖包）
> *   `/php/public/upload/`（用户上传的文件资源）

### 3. 后端编码与逻辑规范
*   **严格类型声明**：所有新创建的 PHP 文件必须在顶部声明严格类型：
    ```php
    declare(strict_types=1);
    ```
*   **数据库迁移**：数据库表结构的变动原则上必须通过 `database/` 下的迁移工具（`php think migrate`）进行，以便团队协作。
*   **安全防范**：
    *   禁止拼接 SQL，必须使用 ThinkPHP 的链式操作或参数绑定防范 SQL 注入。
    *   所有敏感配置（数据库密码、微信 AppID、AppSecret 等）必须从 `.env` 或系统配置中读取，禁止硬编码。
*   **接口统一格式**：所有给前端调用的接口必须返回统一的 JSON 结构，并遵循规范的数据格式。

---

## 🎨 前端开发规范 (`/uni`)

### 1. 技术栈与运行环境
*   **核心框架**：Vue 3 + uni-app (通过 Vite 构建)
*   **开发语言**：TypeScript
*   **状态管理**：Pinia
*   **包管理工具**：pnpm
*   **本地核心依赖**：
    *   `@hlw-uni/mp-vue` -> 指向本地物理路径
    *   `@hlw-uni/mp-vite-plugin` -> 指向本地物理路径

### 2. 核心目录结构
*   `src/core/`：存放业务的组合式逻辑（Hooks/Composables），保持组件代码的轻量化。
*   `src/service/`：API 接口请求模块，统一封装请求与数据解析。
*   `src/store/`：Pinia 状态管理模块。
*   `src/pages/`：小程序页面组件，按业务模块划分。
    *   **页面内小组件规范**：专属的小组件（如仅供当前页面使用）必须且尽量存放在该页面目录下的 `comps/` 文件夹中（例如 `src/pages/user/comps/`）。
    *   **组件命名规范**：小组件文件名必须统一且强制以 `comp-` 开头，命名格式为 `comp-xxx.vue`（例如：`comp-card.vue`）。
    *   **名称极简规范**：尽量使用最简短、清晰的单词来命名组件，切忌长难词。
*   `src/types/`：全局 TypeScript 接口与类型定义文件。

### 3. 前端编码与构建规范
*   **构建模式与环境变量**：
    *   **开发模式**：执行 `pnpm dev`，读取 `.env.dev` 进行构建。
    *   **生产模式**：执行 `pnpm build`，读取 `.env.prod` 进行构建。
    *   > [!WARNING]
        > 上传微信小程序体验版或线上版本前，**必须**运行 `pnpm build` 构建生产版，然后使用微信开发者工具打开 `dist/build/mp-weixin` 进行上传。切勿上传 `dist/dev/mp-weixin` 开发包。
*   **类型安全**：
    *   新增或修改前端代码后，必须运行 `pnpm type-check` 进行 TypeScript 严格类型与 Vue 模版类型检查。
    *   AI 生成代码时，尽量避免使用 `any`，必须合理定义并导出 interfaces/types。
*   **通用交互约定**：
    *   页面内弹出 Toast 消息提示时，**统一且唯一**推荐使用底层封装的公共方法：
        ```typescript
        hlw.$msg.toast('提示内容');
        ```
        禁止直接使用 `uni.showToast()`，以保证交互风格与样式的一致性。
    *   **一键复制功能**：当页面需要提供快捷复制文本或 ID 等功能时，**统一且唯一**强制直接在标签上使用内置的 Vue 自定义复制指令 `v-copy`，例如：
        ```html
        <view class="id-wrapper" v-copy="user?.id">
            <text class="user-id">ID: {{ user?.id }}</text>
        </view>
        ```
        绝对禁止手动绑定点击事件并调用 `uni.setClipboardData` 或底层 API，以保证交互流程的最简化与高复用性。
*   **页面 UI 与样式规范 (纯 Class)**：
    *   **必须使用纯 CSS/SCSS 样式类（纯 class）**编写页面布局与视觉样式。
    *   **绝对禁止**直接在模板元素上堆叠使用 UnoCSS 的原子化工具类（例如禁止使用 `<view class="flex justify-between items-center text-slate-800">` 等）。
    *   应该给组件和视图元素赋予高可读性、符合语义的 Class 名称（例如 `class="user-profile-card"`），并在 `<style scoped lang="scss">` 或对应样式文件中集中编写 CSS/SCSS。
    *   **优先且在有必要的情况下使用 `uni/unocss.config.ts` 自带的内置快捷 Class 属性**：如果项目中已内置了现成的快捷样式类（如 `flex-center`、`col-center`、`flex-between`、`flex-items-center`、`text-ellipsis`、`abs-center`、`btn`、`btn-*`、`tag-*`、`soft-shadow` 等），在有必要的情况下，**必须优先直接使用这些内置的 Class**，避免在 SCSS 中重复书写相同的通用 CSS 样式逻辑，从而保持前端代码和样式的精炼和高效。
    *   **必须统一使用 `rpx` 作为尺寸单位**：在编写各类 CSS/SCSS 样式属性（包括宽高、内边距 `padding`、外边距 `margin`、定位、字体大小等）时，**除极少数需要精确的极细边框外，一律统一且强制使用 `rpx` 响应式像素单位**（严禁随意使用 `px`），以确保小程序在不同宽度的移动端屏幕上能够完美按比例自适应缩放。
*   **能用 CSS 变量的一定要用 CSS 变量**：在开发 `/uni` 小程序时，**凡是能使用 CSS 变量的场景，必须且强制使用 CSS 变量**（如主题色、背景色、文字颜色、字体大小、圆角、内距外距等）。严禁随意硬编码颜色值或尺寸，确保全局设计系统的一致性与易维护性。
*   **CSS 字体大小与字重规范**：
    *   **必须使用字体大小变量**：在编写 CSS/SCSS 的 `font-size` 时，**严禁硬编码字号值**（如 `font-size: 28rpx;` 或 `font-size: 14px;`）。必须使用本项目定义的 CSS/SCSS 变量：
        *   **主题档**（与系统字体偏好联动）：`var(--font-xs)`、`var(--font-sm)`、`var(--font-base)`、`var(--font-md)`、`var(--font-lg)`、`var(--font-xl)`。
        *   **原子档**（精确固定字号，2rpx 步进）：`var(--font-20)` 至 `var(--font-40)`。
        *   **语义化排版 Token**：`var(--text-title-size)`、`var(--text-body-size)`、`var(--text-desc-size)` 等。
    *   **尽量不要使用粗体（字重规范）**：除极少数需要强力突出的核心元素（如页面主标题、醒目的大数字展示）可适当使用中等/中粗字重（如 `font-weight: 500;` 或 `font-weight: 600;`）外，**其他绝大多数常规文本、次级标题、菜单项、辅助说明、标签等一律尽量不加粗**（保持默认字重 `font-weight: 400;` 或 `normal`），避免界面视觉效果显得臃肿，以呈现清爽、现代、高级感的极简视觉风格。
    *   **字体间距推荐**：在为文本元素（如常规文字、标题、说明等）编写样式时，**能加上 `letter-spacing: 1rpx;` 就尽量加上**。这可以通过微调字符间距，使字体在手机屏幕上显得更加舒展、精致和高级，提升整体阅读的高级质感。
*   **CSS 圆角与边距变量（重点）**：
    *   在编写 CSS/SCSS 的 `border-radius`、`padding` 和 `margin` 时，**严禁使用过大的硬编码数值**（例如禁止硬编码 `20px`、`24px` 等大边距），**默认必须使用系统自带的默认边距与圆角变量**，以保证界面精致且全局风格统一：
        *   **圆角变量**：
            *   `var(--radius-sm)` (8rpx)：适用于小元素、小标签、小按钮等。
            *   `var(--radius-md)` (16rpx)：适用于常规按钮、输入框、普通卡片等。
            *   `var(--radius-lg)` (24rpx)：适用于卡片内层、子卡片等。
            *   `var(--radius-xl)` 或 `var(--card-radius)` (32rpx)：适用于典型大卡片外层、弹窗容器等。
            *   `var(--radius-full)` (9999rpx)：适用于胶囊按钮、进度条、圆形头像、Pill 标签等。
        *   **边距/填充变量**：
            *   `var(--page-padding)` (26rpx)：页面最外层容器的系统默认统一内边距。
            *   `var(--card-padding)` (26rpx)：典型卡片的系统默认统一内边距。
*   **图标系统开发规范**：
    *   图标系统采用 `@unocss/preset-icons` 进行静态编译，图标数据源自三个特定的依赖包，必须且仅使用以下前缀的图标：
        *   `@iconify-json/fa6-solid`：前缀为 `i-fa6-solid-` 或 `i-fa6-solid:`（例如 `i-fa6-solid-gear`）
        *   `@iconify-json/fa6-brands`：前缀为 `i-fa6-brands-` 或 `i-fa6-brands:`（例如 `i-fa6-brands-weixin`）
        *   `@iconify-json/ri`：前缀为 `i-ri-` 或 `i-ri:`（例如 `i-ri-apps-fill`）

---

## 🤝 前后端协作与接口规范

*   **命名风格一致性**：
    *   前后端交互的接口字段、数据库表字段、请求体与返回体字段，统一采用 **下划线 (snake_case)** 风格。
    *   *示例*：
        *   正例：`video_url`、`source_url`、`user_id`
        *   反例：`videoUrl`、`sourceUrl`、`userId`
*   **时间格式**：所有接口返回的时间字段，原则上统一返回秒级时间戳或标准 `YYYY-MM-DD HH:mm:ss` 格式字符串。
*   **状态码设计**：
    *   成功响应必须返回统一的状态码。
    *   失败响应需提供友好的错误提示 `msg`。

---

## 🤖 给 AI 助手的黄金准则

1.  **绝不使用占位符**：不要在代码中遗留 `// TODO`、`// 后面省略`、`...` 等占位符。必须提供直接可用、逻辑完整的完整实现。
2.  **严格遵循现有目录结构**：根据后端 (`php/`) 或前端 (`uni/`) 的既定技术栈设计代码，禁止引入不相符的第三方库或改变既定架构。
3.  **遵守命名规约与极简命名原则**：
    *   无论是 API 请求参数、响应参数，还是数据库字段，AI 必须自动遵守 `snake_case` 命名。
    *   **组件、变量、方法/函数极简原则**：所有的组件、变量、方法命名必须秉持**极简、直接**的原则，尽量使用简短、高频且含义清晰的英文单词，不要搞得太复杂或过于冗长（例如：用 `getUser` 代替 `fetchUserDetailInfoFromServer`，用 `status` 代替 `currentAccountStatusValue`）。
4.  **优雅的异常处理**：
    *   后端方法应有完备的 try-catch，并记录日志或向前端抛出清晰的错误。
    *   前端网络请求与异步操作必须有 catch 逻辑，并通过 `hlw.$msg.toast()` 进行全局提示。
5.  **配合环境配置**：所有可能变动的常量都必须引导用户通过 `.env` 相关的环境变量文件进行配置，不得硬编码在代码中。
6.  **禁止清空或删除运行时目录**：
    *   **红线要求**：**绝对禁止**清空、删除或重置 `/php/runtime/` 目录下的缓存与会话文件。该目录保留了用户的当前登录 Session 与运行时缓存，一旦被清空或删除，用户将被迫退出登录并必须重新登录。在执行任何清理、脚本重构、初始化或维护操作时，必须严格跳过并保护该目录的完整性。
7.  **Git 提交、版本递增与推送规范**：
    *   **常规修改阶段**：当日常编写、修改、优化代码时，**绝对不要**自动修改版本配置文件中的版本号，也**不要**自动进行 Git 提交。
    *   **触发提交阶段**：只有当用户明确要求提交 Git（例如，用户说“帮我提交git”）时，才允许并执行以下版本递增、提交、推送及更新流程：
        1.  **修改版本号**：在准备提交 Git 之前，**必须**先检查并递增该仓库对应版本配置文件中的版本号（如前端项目的 `/uni/package.json` 或独立 PHP 插件子仓库目录下的 `composer.json` 等中的 `"version"` 字段）。
            *   版本号格式遵循 `主版本号.次版本号.修订号`（即 `X.Y.Z`）。
            *   修订号 `Z` 不进行任何零补位（只能为 `0` - `9` 的单数字，如 `1.0.1`、`1.0.2` 等）。
            *   **常规递增**：在当前修订号 `Z` 未达到 `9` 时，将修订号 `Z` 递增 1（例如：`1.0.8` 变为 `1.0.9`）。
            *   **特殊进位规则**：当修订号 `Z` 达到 `9` 时（例如 `1.0.9`），升级版本后修订号 `Z` 重置为 `0`，且次版本号 `Y` 递增 1（例如：`1.0.9` 升级后必须是 `1.1.0`）。
        2.  **提交 Git**：将版本号的升级与本次实际代码修改合并在同一个 Git 提交中。
            *   **强制中文 Commit 规范**：每次提交 Git 代码时，**必须且强制使用极其详尽的中文说明**编写提交信息（Commit Message），**绝对且严厉禁止使用纯英文或英文首字母缩写编写提交说明**（技术词汇、文件名、组件名除外）。
            *   提交信息中必须包含：**修改的核心模块/组件/文件**，以及**本次修改的具体原因和变动细节（必须使用清晰、直观的中文分条或串联说明）**。如果涉及版本升级，需包含**升级后的版本号**。
            *   *标准中文 Commit 示例*：
                *   `修补: 优化 user-profile 个人中心布局 & 统一使用 --font-lg 字体变量 & 调整 stat-value 字号与间距`
                *   `功能: 客服组件 hlw-custom-service 支持本地图片与网络 URL 图标路径 & 修复 WXSS 编译时中文全角逗号引发的报错问题 & 升级版本至 1.0.2`
        3.  **推送 Git**：在完成本地提交（`git commit`）后，**必须**自动或按流程执行 `git push` 将本地改动推送到云端远程仓库。
        4.  **更新 NPM**：若当前项目或所修改的插件属于公开/私有 npm 包，或者依赖关系需要更新，则在推送 Git 后，执行对应的 NPM 发布或更新命令（如 `npm publish` / `pnpm publish`，或更新其他项目中对应的包依赖）。
