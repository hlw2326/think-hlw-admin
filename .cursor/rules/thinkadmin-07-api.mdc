---
description: ThinkAdmin v6 API 接口规范：小程序前端接口、广告配置接口、通知公告接口
alwaysApply: true
---

# ThinkAdmin v6 API 接口规范

## 一、API 控制器目录

**mini 模块**：API 控制器统一放在 `app/mini/controller/api/v1/` 目录，命名空间为 `app\mini\controller\api\v1`。
**qz 插件**：API 控制器放在 `package/think-plugin-qz/stc/controller/api/v1/` 目录，命名空间为 `package\think-plugin-qz\controller\api\v1`，模型使用 `QzAd`、`QzNotice`、`QzMp` 等。

## 二、接口路由

| 接口 | 路由 | 说明 |
|------|------|------|
| 广告配置 | `/api/v1/ad/config?appid=xxx` | 获取小程序广告单元配置 |
| 通知列表 | `/api/v1/notice/list?appid=xxx&type=xxx` | 获取通知公告列表 |
| 登录 | `/api/v1/login/in` | 小程序登录 |

## 三、广告配置接口

### 路由

```
GET /api/v1/ad/config?appid=xxx
```

### 控制器

```php
<?php
declare(strict_types=1);

namespace app\mini\controller\api\v1;

use app\mini\model\MiniAd;
use package\think-plugin-qz\model\QzAd;
use think\admin\Controller;

/**
 * 广告配置接口
 * @class Ad
 */
class Ad extends Controller
{
    /**
     * 获取指定小程序的广告单元配置
     */
    public function config(): void
    {
        $appid = $this->request->get('appid', '');
        if (empty($appid)) {
            $this->error('appid 不能为空');
        }

        $list = QzAd::mk()
            ->where('appid', $appid)
            ->where('status', 1)
            ->order('sort desc, id asc')
            ->field('id,name,unit_id,ad_type,position,extra')
            ->select()->toArray();

        $map = [];
        foreach ($list as &$item) {
            $item['extra'] = $item['extra'] ? json_decode($item['extra'], true) : [];
            $map[$item['position']] = $item;
        }

        $this->success('获取成功', ['list' => $list, 'map' => $map]);
    }
}
```

### 返回结构

```json
{
    "code": 1,
    "info": "获取成功",
    "data": {
        "list": [
            {
                "id": 1,
                "name": "首页顶部广告",
                "unit_id": "adunit-xxxxxxxxxxxxxxxx",
                "ad_type": "banner",
                "position": "home_top",
                "extra": {"interval": 60}
            }
        ],
        "map": {
            "home_top": {
                "id": 1,
                "unit_id": "adunit-xxxxxxxxxxxxxxxx",
                "ad_type": "banner",
                "extra": {"interval": 60}
            }
        }
    }
}
```

> `list` 返回所有广告记录（便于前端展示管理），`map` 以 `position` 为键便于按位置快速查找。只返回 `status=1` 的记录。

## 四、通知公告接口

### 路由

```
GET /api/v1/notice/list?appid=xxx&type=xxx
```

### 控制器

```php
<?php
declare(strict_types=1);

namespace app\mini\controller\api\v1;

use app\mini\model\MiniNotice;
use package\think-plugin-qz\model\QzNotice;
use think\admin\Controller;

/**
 * 通知公告接口
 * @class Notice
 */
class Notice extends Controller
{
    /**
     * 获取通知列表
     */
    public function list(): void
    {
        $appid = $this->request->get('appid', '');
        $type  = $this->request->get('type', '');

        $query = QzNotice::mk()->where('status', 1)->where('appid', $appid);
        if ($type !== '') {
            $query->where('type', $type);
        }
        $list = $query->order('sort desc,id desc')->select()->toArray();

        $this->success('获取成功', ['list' => $list]);
    }
}
```

### 返回结构

```json
{
    "code": 1,
    "info": "获取成功",
    "data": {
        "list": [
            {
                "id": 1,
                "type": "system",
                "title": "系统通知",
                "content": "通知内容",
                "jump_type": "navigateTo",
                "jump_value": "/pages/index/index"
            }
        ]
    }
}
```

## 五、登录接口

```php
<?php
declare(strict_types=1);

namespace app\mini\controller\api\v1;

use app\mini\model\MiniMp;
use package\think-plugin-qz\model\QzMp;
use think\admin\Controller;
use WeMini\Crypt;

/**
 * 登录接口
 * @class Login
 */
class Login extends Controller
{
    public function in(): void
    {
        if ($this->request->isPost()) {
            $code  = $this->request->post('code');
            $appid = $this->request->get('appid');
            $mp = QzMp::mk()->where(['appid' => $appid])->findOrEmpty();
            $config = [
                'appid'          => $mp->appid,
                'appsecret'      => $mp->appsecret,
                'encodingaeskey' => '',
            ];
            $crypt = Crypt::instance($config);
            $this->success('登录成功', $crypt);
        }
    }
}
```

## 六、API 返回规范

### 成功返回

```php
$this->success('操作成功', $data);
```

### 失败返回

```php
$this->error('错误信息');
```

ThinkAdmin 的 Controller 基类提供 `$this->success()` 和 `$this->error()` 方法，自动返回框架标准的 JSON 格式。

### 标准响应格式

```json
{
    "code": 1,
    "info": "操作成功",
    "data": {}
}
```

## 七、接口鉴权

目前 mini 模块 API 接口通过 `appid` 参数识别小程序，不做强制鉴权。业务逻辑中的用户操作（如查询、充值）依赖后端业务逻辑本身的安全校验。

如需加强鉴权，可在路由层或中间件中处理。
