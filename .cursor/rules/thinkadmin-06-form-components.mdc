---
description: ThinkAdmin v6 表单组件规范：图片上传、form 字段规范、sort/status 内联编辑、流量主广告模块
alwaysApply: true
---

# ThinkAdmin v6 表单组件规范

## 一、图片上传组件写法

### 标准写法

```html
<div class="relative block">
    <label>
        <input class="layui-input layui-bg-gray" readonly placeholder="请上传图片"
               data-tips-hover data-tips-image name="cover" value='{$vo.cover|default=""}'>
    </label>
    <a class="layui-icon layui-icon-upload input-right-icon"
       data-file="image" data-field="cover" data-type="gif,png,jpg"></a>
</div>
```

### 关键属性说明

| 属性 | 说明 |
|------|------|
| `data-file="image"` | 打开**图片空间**选择图片（推荐） |
| `data-file` 或 `data-file="btn"` | 直接**本地上传**文件 |
| `data-field="xxx"` | 上传/选择完成后自动填入 `name="xxx"` 的 input |
| `data-type="gif,png,jpg"` | 限制允许的文件类型 |
| `data-tips-image` | 鼠标悬停时预览图片 |
| `data-tips-hover` | 配合 `data-tips-image` 使用 |
| `readonly` + `layui-bg-gray` | input 只读灰色背景 |

### 列表页图片列

**禁止**单独定义 `XxxImageTpl` 模板块，直接用 `$.layTable.showImage` 行内写法：

```javascript
// ✅ 正确
{field: 'logo', title: 'Logo', width: 65, align: 'center', templet: '<div>{{-$.layTable.showImage(d.logo)}}</div>'}

// ❌ 禁止
{field: 'logo', title: 'Logo', width: 120, align: 'center', templet: '#LogoTpl'}
```

`width` 推荐 `65`（与头像等图片列保持一致）。

---

## 二、form.html 不放的字段规范

以下字段**统一不放在 `form.html` 表单里**：

| 字段 | 原因 | 替代方式 |
|------|------|---------|
| `sort` 排序权重 | 列表页内联直接编辑更高效 | `SortInputTpl` + 框架默认 sort() |
| `status` 状态 | 列表页 switch 开关更直观 | `StatusSwitchTpl` + 控制器 `state()` |

**控制器不需要定义 `sort()` 方法**，框架底层已默认实现，**一律删除**。

### sort 内联编辑

```javascript
// index.html cols 定义
{field: 'sort', title: '{:lang("排序")}', width: 120, align: 'center', templet: '#SortInputTpl'}
```

```html
<!-- SortInputTpl 模板 -->
<script type="text/html" id="SortInputTpl">
    <input type="number" data-blur-number="0"
           data-action-blur="{:sysuri()}"
           data-value="id#{{d.id}};action#sort;sort#{value}"
           data-loading="false" value="{{d.sort}}" class="layui-input text-center">
</script>
```

### state() 必须显式定义

```php
public function state(): void
{
    MiniXxx::mSave($this->_vali([
        'status.in:0,1'   => '状态值范围异常！',
        'status.require'  => '状态值不能为空！',
    ]));
}
```

---

## 三、流量主广告模块规范（mini_ad）

### 广告类型枚举

```php
public static function getAdTypes(): array
{
    return [
        'banner'        => ['label' => 'Banner '        . lang('横幅广告'), 'class' => 'layui-bg-blue'],
        'video'         => ['label' => 'Video '         . lang('视频贴片'), 'class' => 'layui-bg-cyan'],
        'interstitial'  => ['label' => 'Interstitial '  . lang('插屏广告'), 'class' => 'layui-bg-orange'],
        'rewarded_video'=> ['label' => 'RewardedVideo ' . lang('激励视频'), 'class' => 'layui-bg-green'],
        'box'           => ['label' => 'Box '           . lang('格子广告'), 'class' => 'layui-bg-gray'],
    ];
}
```

### 表结构关键字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `appid` | varchar(50) | 关联 `mini_mp.appid`，不用外键 |
| `unit_id` | varchar(100) | 微信流量主后台生成，格式 `adunit-xxxxxxxxxxxxxxxx` |
| `ad_type` | varchar(30) | 广告类型，枚举见上 |
| `position` | varchar(50) | 自定义位置标识，前端按此取配置 |
| `extra` | text | 扩展参数 JSON |

### position 命名约定

| 示例值 | 说明 |
|--------|------|
| `home_top` | 首页顶部 |
| `home_bottom` | 首页底部 |
| `detail_bottom` | 详情页底部 |
| `task_rewarded` | 任务页激励视频 |

格式：`{页面}_{位置/类型}`，全小写下划线，同一 appid 下 `position` 值唯一。

### 列表页 JS 辅助函数（badge）

```javascript
$.adTypeLabel = (function () {
    var types = JSON.parse($('#AdTypesData').text());
    return function (type) {
        var item = types[type] || {label: type, 'class': 'layui-bg-gray'};
        return '<span class="layui-badge ' + item['class'] + '">' + item.label + '</span>';
    };
})();
```

hidden textarea id：`AdTypesData`、`MpListData`。

### 前端 API 接口

```
GET /api/v1/ad/config?appid=wx_your_appid
```

返回结构：
```json
{
  "list": [...],
  "map": {
    "home_top":      { "unit_id": "adunit-xxx", "ad_type": "banner", "extra": {} },
    "detail_bottom": { "unit_id": "adunit-yyy", "ad_type": "box",    "extra": {} }
  }
}
```

只返回 `status=1` 的记录，`extra` 字段自动 `json_decode` 为对象，前端直接使用。

---

## 四、index_search.html 字段值传递

**禁止在模板中直接写死枚举选项**，控制器传入枚举数组，模板 `{foreach}` 循环：

```php
// 控制器
$this->field_labels = MiniUserLog::getFieldLabels();
```

```html
<select name="field" class="layui-select" lay-append-to>
    <option value="">-- {:lang('全部')} --</option>
    {foreach $field_labels as $k => $v}
    <option value="{$k}" {if isset($get.field) && $get.field == $k}selected{/if}>{$v}</option>
    {/foreach}
</select>
```

**禁止在模板中调用业务模型静态方法**：

```html
<!-- ❌ 禁止 -->
<textarea id="XxxData">{:json_encode(\app\mini\model\MiniXxx::getItems(), JSON_UNESCAPED_UNICODE)}</textarea>

<!-- ✅ 正确：控制器赋值 -->
<!-- $this->xxx = MiniXxx::getItems(); -->
<textarea id="XxxData">{:json_encode($xxx, JSON_UNESCAPED_UNICODE)}</textarea>
```
