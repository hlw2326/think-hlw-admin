---
description: ThinkAdmin v6 Package 插件开发规范：目录结构、composer.json、命名空间、模型写法、视图继承、path 仓库配置、配置 key 命名
alwaysApply: true
---

# ThinkAdmin v6 Package 插件开发规范

## 一、目录结构

```
package/think-plugin-collect/
├── composer.json          # 插件元信息
├── stc/
│   └── database/         # 迁移文件（发布时复制到 database/migrations）
│       └── YYYYMMDDSSSS_install_collect_tag.php
└── src/
    ├── Service.php        # 注册服务（services、copy 等）
    ├── controller/        # 控制器，namespace: plugin\{name}\controller
    ├── model/             # 模型，namespace: plugin\{name}\model
    └── view/              # 视图，extends 用 "table"/"main"（相对本目录）
        ├── table.html     # 列表页基础模板
        ├── main.html      # 整页基础模板
        ├── tag/
        │   ├── index.html
        │   ├── index_search.html
        │   └── form.html
        └── query_log/
            ├── index.html
            └── detail.html
```

---

## 二、composer.json 关键字段

```json
{
    "type": "think-admin-plugin",
    "name": "hlw2326/think-plugin-collect",
    "autoload": {
        "psr-4": {
            "plugin\\collect\\": "src"
        }
    },
    "extra": {
        "think": {
            "services": ["plugin\\collect\\Service"]
        },
        "plugin": {
            "copy": {
                "stc/database": "database/migrations"
            }
        }
    }
}
```

---

## 三、命名空间规范

| 组件 | 命名空间 | 示例 |
|------|----------|------|
| 控制器 | `plugin\{name}\controller` | `plugin\collect\controller\Tag` |
| 模型 | `plugin\{name}\model` | `plugin\collect\model\PluginCollectTag` |
| 服务 | `plugin\{name}` | `plugin\collect\Service` |

**模型类名**：用 `Plugin{Name}` 命名，如 `PluginCollectTag`、`PluginCollectCookie`，避免与主项目模型名冲突。

---

## 四、插件模型写法

```php
<?php
declare(strict_types=1);

namespace plugin\collect\model;

use think\admin\Model;

/**
 * 标签管理模型
 * @property int $id 主键ID
 * @property string $name 标签分类名称
 * @property string $value 子标签（逗号分隔）
 * @property int $sort 排序权重
 * @property int $status 状态(0禁用,1启用)
 * @property string $create_at 创建时间
 * @property string $update_at 更新时间
 * @class PluginCollectTag
 */
class PluginCollectTag extends Model
{
    /** @var string 表名（显式指定，与类名不一致时必须写） */
    protected $table = 'plugin_collect_tag';

    /** @var bool 自动写入时间戳 */
    protected $autoWriteTimestamp = true;

    protected $createTime = 'create_at';
    protected $updateTime = 'update_at';
}
```

---

## 五、视图继承路径

**插件视图不用 admin 路径**，直接用相对于 `src/view/` 的名称：

| 页面类型 | 正确写法 |
|----------|----------|
| 列表页 | `{extend name="table"}` |
| 整页表单（data-open） | `{extend name="main"}` |
| 弹窗表单（data-modal） | 不继承，写完整独立 HTML |
| 详情页 | 不继承，写完整独立 HTML |

**include 路径**：同样相对于 `src/view/` 根目录，如 `{include file='tag/index_search'}`。

**标准 form.html 弹窗（不继承，独立结构）：**

```html
<form action="{:sysuri()}" method="post" data-auto="true" class="layui-form layui-card" data-table-id="XxxTable">
    <div class="layui-card-body padding-left-40">
        <fieldset class="layui-bg-gray">
            <legend><b class="layui-badge think-bg-violet">{:lang('基本信息')}</b></legend>
            <div class="layui-row layui-col-space15">
                <div class="layui-col-xs6">
                    <label class="block relative">
                        <span class="help-label"><b>{:lang('名称')}</b>Name</span>
                        <input name="name" value='{$vo.name|default=""}' required vali-name="{:lang('名称')}" class="layui-input">
                    </label>
                </div>
            </div>
        </fieldset>
    </div>
    <div class="hr-line-dashed"></div>
    {notempty name='vo.id'}<input type='hidden' value='{$vo.id}' name='id'>{/notempty}
    <div class="layui-form-item text-center">
        <button class="layui-btn" type='submit'>{:lang('保存数据')}</button>
        <button class="layui-btn layui-btn-danger" type='button' data-confirm="{:lang('确定要取消编辑吗？')}" data-close>{:lang('取消编辑')}</button>
    </div>
</form>
```

---

## 六、根 composer.json 的 path 仓库配置

根 `composer.json` 中的 path 仓库配置，需要 **exclude** packagist 上已发布的同名包，防止 Composer 优先从远程仓库安装而非本地开发：

```json
{
    "repositories": {
        "packagist": {
            "type": "composer",
            "url": "https://packagist.org",
            "exclude": [
                "hlw2326/think-plugin-collect",
                "hlw2326/think-plugin-backup"
            ]
        },
        "plugincollect": {
            "type": "path",
            "url": "package/think-plugin-collect"
        },
        "pluginbackup": {
            "type": "path",
            "url": "package/think-plugin-backup"
        }
    }
}
```

修改后需执行 `composer dump-autoload` 生效。

---

## 七、插件配置 key 命名规范

插件配置 key 必须以 `plugin_{name}.` 为前缀，便于识别归属：

```php
// ✅ 正确
sysconf('plugin_collect.default_platform')
sysconf('plugin_collect.request_timeout')

// ❌ 错误
sysconf('collect.default_platform')
```

视图中的 name 属性同理：

```html
<input name="plugin_collect.request_timeout" value='{:sysconf("plugin_collect.request_timeout", 30)}'>
```

---

## 八、插件 Service 注册

```php
<?php
declare(strict_types=1);

namespace plugin\collect;

use think\Service;

class Service extends Service
{
    public function register(): void
    {
        // 自动加载 autoload
    }

    public function boot(): void
    {
        // 应用启动后执行
    }
}
```
