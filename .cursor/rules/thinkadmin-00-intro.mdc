---
description: ThinkAdmin v6 基础规范：技术栈、模块权限、admin 功能总览
alwaysApply: true
---

# ThinkAdmin v6 项目基础规范

## 技术栈

- **框架**：ThinkPHP 6 + ThinkAdmin v6
- **模板引擎**：ThinkTemplate（不是 Blade，不是 Twig）
- **前端**：Layui 2.x（不是 Bootstrap，不是 Element UI）
- **数据库**：MySQL
- **参考模块**：`app/admin/`（标准实现）、`app/wechat/`（业务参考）、`app/mini/`（当前业务）

---

## 模块修改权限

| 模块 | 路径 | 是否可修改 |
|------|------|-----------|
| admin 管理模块 | `app/admin/` | ❌ **禁止修改** |
| wechat 微信模块 | `app/wechat/` | ❌ **禁止修改** |
| mini 小程序模块 | `app/mini/` | ✅ 当前业务模块 |
| package 插件目录 | `package/` | ✅ 可扩展插件目录（`think-plugin-collect`、`think-plugin-backup`） |

> `app/admin/` 和 `app/wechat/` 是框架固定实现，仅作为**参考范本**，不得改动任何文件。

---

## admin 模块功能一览

`app/admin/` 是 ThinkAdmin 的管理后台核心，提供以下标准功能模块。遇到实现问题时，先查阅 `app/admin/` 中是否有现成实现。

### 内容管理类

| 模块 | 路径 | 说明 |
|------|------|------|
| 文章管理 | `app/admin/controller/article/` | 支持文章分类、内容发布 |
| 单页管理 | `app/admin/controller/article/` | 独立页面管理 |
| 文章分类 | `app/admin/controller/Cate` | 内容分类（树形结构） |
| 附件管理 | `app/admin/controller/Upload` | 上传文件管理 |

### 系统配置类

| 模块 | 路径 | 说明 |
|------|------|------|
| 系统配置 | `app/admin/controller/Config` | `sysconf()` 读写全局配置 |
| 存储配置 | `app/admin/controller/Storage` | 文件存储驱动配置 |
| 系统日志 | `app/admin/controller/Log` | 操作日志查看 |
| 数据备份 | `think-plugin-backup` 插件 | 数据库备份（参考 `package/think-plugin-backup/`） |
| 菜单管理 | `app/admin/controller/Menu` | 后台菜单配置 |

### 用户权限类

| 模块 | 路径 | 说明 |
|------|------|------|
| 登录/登出 | `app/admin/controller/Index` | 管理员登录认证 |
| 用户管理 | `app/admin/controller/User` | 后台用户 CRUD |
| 用户等级 | `app/admin/controller/Level` | 用户等级设置 |
| 权限节点 | `app/admin/controller/Node` | 权限规则节点 |

### 标准控制器方法（参考）

每个业务模块应参照 `app/admin/` 的实现方式，实现以下标准方法：

```php
class Xxx extends Controller
{
    public function index(): void   // 列表页（layTable）
    public function add(): void     // 新增（弹窗 mForm）
    public function edit(): void    // 编辑（弹窗 mForm）
    public function state(): void   // 修改状态（switch，mSave）
    public function remove(): void  // 删除（mDelete，批量）
}
```

---

## 常用系统函数

| 函数 | 说明 |
|------|------|
| `sysconf('key')` | 读取系统配置 |
| `sysconf('key', $value)` | 写入系统配置 |
| `sysdata('key')` | 读取结构化数据（JSON） |
| `sysdata('key', $array)` | 写入结构化数据 |
| `sysoplog('模块', '描述')` | 记录操作日志 |
| `sysuri('index')` | 生成当前模块 URL |
| `runtime_path()` | 获取 runtime 目录路径 |
| `lang('key')` | 多语言翻译 |
| `AdminService::getUserId()` | 当前管理员 ID |
| `AdminService::isSuper()` | 是否超级管理员 |
| `AdminService::isLogin()` | 是否已登录 |
