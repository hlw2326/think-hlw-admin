---
description: ThinkAdmin v6 数据库规范、Phinx 迁移文件规范、工具类速查（CodeExtend/AdminService/DataExtend/HttpExtend）、常见错误速查
alwaysApply: true
---

# ThinkAdmin v6 数据库与迁移规范

## 一、数据库建表规范

```sql
CREATE TABLE `mini_xxx` (
    `id`        int(11) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `name`      varchar(255) NOT NULL DEFAULT '' COMMENT '名称',
    `status`    tinyint(1) NOT NULL DEFAULT 1 COMMENT '状态(0禁用,1启用)',
    `sort`      int(11) NOT NULL DEFAULT 0 COMMENT '排序权重',
    `create_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='权重-用户列表';
```

### 字段类型规范

| 场景 | 类型 | 命名 |
|------|------|------|
| 字符串 | `varchar` | 超长才用 `text` |
| 金额 | `decimal(10,2)` | 不用 `float` |
| 状态/布尔 | `tinyint(1)` | 0=禁用，1=启用 |
| 记录创建时间 | `timestamp` | `create_at`（自动写入） |
| 记录更新时间 | `timestamp` | `update_at`（自动更新） |
| 业务时间戳 | `int(11)` | 以 `_time` 结尾，0=未发生 |
| JSON 数据 | `text` | 用模型的 `$json` 配置自动序列化 |
| IP 地址 | `varchar(50)` | 存字符串 |

- 所有字段必须有 `COMMENT`
- 不使用外键，关联在代码层处理

---

## 二、Phinx 迁移文件规范

### 存放位置

迁移文件必须放在项目根目录的 `database/migrations/` 下，**不是** `stc/database/`。

### 文件命名规范

格式：`YYYYMMDDHHNNSS_{action}_{table_name}.php`

- `YYYYMMDDHHNNSS`：时间戳，精确到秒，同一秒内多条按序号递增
- `action`：建表用 `install`，添加字段用 `add`，修改字段用 `alter`，删除表用 `drop`
- `table_name`：目标表名下划线格式

示例：
- `20260408000001_install_mini_mp_cate.php`
- `20260408000002_add_mini_mp_menu_cate_id.php`

### 标准迁移文件结构

```php
<?php

declare(strict_types=1);

use think\admin\extend\PhinxExtend;
use think\migration\Migrator;

@set_time_limit(0);
@ini_set('memory_limit', '-1');

class InstallMiniMpCate extends Migrator
{
    /**
     * 创建小程序菜单分类表
     */
    public function up(): void
    {
        $table = $this->table('mini_mp_cate', [
            'engine' => 'InnoDB',
            'collation' => 'utf8mb4_general_ci',
            'comment' => '小程序菜单分类表',
        ]);

        PhinxExtend::upgrade($table, [
            [
                'name',
                'string',
                ['limit' => 100, 'default' => '', 'null' => false, 'comment' => '分类名称']
            ],
            [
                'logo',
                'string',
                ['limit' => 200, 'default' => '', 'null' => false, 'comment' => '分类图标']
            ],
            [
                'sort',
                'integer',
                ['limit' => 10, 'default' => 0, 'null' => false, 'comment' => '排序权重']
            ],
            [
                'status',
                'integer',
                ['limit' => 1, 'default' => 1, 'null' => false, 'comment' => '状态(0禁用,1启用)']
            ],
            [
                'create_at',
                'timestamp',
                ['default' => 'CURRENT_TIMESTAMP', 'null' => false, 'comment' => '创建时间']
            ],
            [
                'update_at',
                'timestamp',
                ['default' => 'CURRENT_TIMESTAMP', 'null' => false, 'comment' => '更新时间']
            ],
        ], [
            'status',
            'sort',
            'create_at',
        ]);
    }

    /**
     * 回滚时删除表
     */
    public function down(): void
    {
        $this->table('mini_mp_cate')->drop();
    }
}
```

### 关键注意事项

- `create_at` 和 `update_at` 必须用 `timestamp` 类型，**不能用 datetime**
- 时间字段的默认值固定为 `'CURRENT_TIMESTAMP'`，`null` 为 `false`
- **禁止使用 `'update' => true`**：`update_at` 字段不要写 `'update' => true`，Phinx 不支持该选项，会导致迁移失败
- **禁止 datetime 使用 `default: null`**：`datetime` 类型不要写 `'default' => null`，MySQL 不支持 `DEFAULT NULL` 语法，**直接省略 default 即可**
- **积分/金额/大数值字段用 `biginteger`**：积分（score）、用户ID、管理员ID 等可能超过 int(11) 范围的字段，统一用 `biginteger`
- **需要软删除的表添加 `deleted` 字段**：`['deleted', 'integer', ['limit' => 1, 'default' => 0, 'null' => true, 'comment' => '删除状态(0未删,1已删)']]`
- 状态字段用 `integer` + `limit: 1`，**不用 boolean**
- `collation` 统一用 `utf8mb4_general_ci`，不用 `utf8mb4_0900_ai_ci`
- 添加字段时需判断字段是否已存在（`hasColumn`），避免重复执行报错
- 添加字段用 `PhinxExtend::addcolumn`，添加索引用 `PhinxExtend::addIndex`

#### 插件表命名规范

插件模块的表名统一以 `plugin_` + 插件名 + `_` 为前缀：

| 插件 | 表前缀 | 示例 |
|------|--------|------|
| qz | `plugin_qz_` | `plugin_qz_user`、`plugin_qz_card_score`、`plugin_qz_card_vip` |

#### 正确示例

```php
// ✅ timestamp 类型（create_at / update_at）
['create_at', 'timestamp', ['default' => 'CURRENT_TIMESTAMP', 'null' => false, 'comment' => '创建时间']],
['update_at', 'timestamp', ['default' => 'CURRENT_TIMESTAMP', 'null' => false, 'comment' => '更新时间']],

// ✅ datetime 类型（可空，无需默认值）
['login_at', 'datetime', ['null' => true, 'comment' => '登录时间']],

// ✅ biginteger 用于 ID、积分等大数值
['id', 'biginteger', ['limit' => 20, 'default' => 0, 'null' => false, 'comment' => '主键ID']],
['score', 'biginteger', ['limit' => 20, 'default' => 0, 'null' => true, 'comment' => '积分']],
['user_id', 'biginteger', ['limit' => 20, 'default' => 0, 'null' => true, 'comment' => '用户ID']],

// ✅ 软删除字段
['deleted', 'integer', ['limit' => 1, 'default' => 0, 'null' => true, 'comment' => '删除状态(0未删,1已删)']],

// ❌ 错误示例
['update_at', 'timestamp', [..., 'update' => true, ...]],  // ❌ update 参数不支持
['login_at', 'datetime', ['default' => null, ...]],       // ❌ datetime 不支持 DEFAULT NULL
```

### 添加字段迁移示例

```php
class AddMiniMpMenuCateId extends Migrator
{
    public function up(): void
    {
        $table = $this->table('mini_mp_menu');

        if (!$table->hasColumn('cate_id')) {
            PhinxExtend::addcolumn($table, 'cate_id', 'integer', [
                'limit' => 10,
                'default' => 0,
                'null' => false,
                'after' => 'path',
                'comment' => '分类ID',
            ]);
            PhinxExtend::addIndex($table, 'cate_id');
        }
    }

    public function down(): void
    {
        $table = $this->table('mini_mp_menu');
        if ($table->hasColumn('cate_id')) {
            $table->removeColumn('cate_id')->save();
        }
    }
}
```

---

## 三、工具类速查

### CodeExtend（随机码/编解码）

```php
use think\admin\extend\CodeExtend;

// 批次号：日期型，带前缀（推荐）
CodeExtend::uniqidDate(16, 'P')    //  P20250330143012xx

// 随机码：type=1纯数字 type=2纯字母 type=3混合
CodeExtend::random(24, 3)

// UUID
CodeExtend::uuid()

// 加解密（AES-256-CBC）
CodeExtend::encrypt($data, $skey)
CodeExtend::decrypt($str, $skey)

// Base64Url 安全编解码
CodeExtend::enSafe64($text)
CodeExtend::deSafe64($text)
```

### AdminService（管理员）

```php
use think\admin\service\AdminService;

AdminService::getUserId()      // 当前管理员ID，替代不存在的 admin_id()
AdminService::isLogin()        // 是否已登录
AdminService::isSuper()        // 是否超级管理员
AdminService::getUserTheme()   // 当前主题
```

### DataExtend（数组/树形）

```php
use think\admin\extend\DataExtend;

DataExtend::arr2tree($list, 'id', 'pid', 'sub')    // 二维数组转嵌套树
DataExtend::arr2table($list)                         // 转带缩进扁平树（含 spt/spl 字段）
DataExtend::getArrSubIds($list, $value)              // 获取子孙ID集合
```

### HttpExtend（HTTP 请求）

```php
use think\admin\extend\HttpExtend;

HttpExtend::get($url, $query, $options)
HttpExtend::post($url, $data, $options)
```

### 全局函数

```php
sysoplog('操作模块', '操作描述');       // 写操作日志
sysdata('key');                          // 读结构化数据（JSON）
sysdata('key', $array);                 // 写结构化数据
sysconf('storage.type');                // 读系统配置
sysconf('key', $value);                 // 写系统配置
sysuri('index');                        // 生成当前模块 URL
runtime_path();                          // 获取 runtime 目录路径
lang('key');                             // 多语言翻译
input('name');                          // 获取请求参数（等同于 request()->get('name')）
input('name/d', 0);                     // 带默认值和类型转换
```

---

## 四、事务处理

### 标准事务写法

```php
try {
    \think\facade\Db::startTrans();

    // 业务逻辑 1
    MiniUser::mk()->where(['id' => $userId])->update(['score' => $newBalance]);

    // 业务逻辑 2
    MiniUserScoreLog::record($userId, $score, $newBalance, 'card_score', $cardId, '卡密充值');

    \think\facade\Db::commit();
    $this->success('操作成功');
} catch (\Exception $e) {
    \think\facade\Db::rollback();
    $this->error('操作失败：' . $e->getMessage());
}
```

---

## 五、常见错误速查

| 错误现象 | 原因 | 修复 |
|----------|------|------|
| `syntax error: unexpected token "default"` | `{if}` 里用了 `\|default=` | 改用 `isset()` 判断 |
| 模板404/找不到 | 继承路径写成 `name='table'` | 改为 `name="../../admin/view/table"` |
| `Call to undefined function admin_id()` | `admin_id()` 不存在 | 改用 `AdminService::getUserId()` |
| 表单提交后列表不刷新 | `data-table-id` 与 `<table id>` 不一致 | 保持完全一致 |
| 新建时 id 被提交为空串 | id 字段无条件输出 | 用 `{notempty name='vo.id'}` 包裹 |
| JS 模板报 PHP 解析错误 | `{:lang()}` 嵌套在双引号内 | 提取为 JS 变量 |
| 时间字段存储/显示混乱 | POST/GET 未做时间戳转换 | POST 用 `strtotime()`，GET 用 `date()` |
| 批次号重复或格式混乱 | 用 `date('YmdHis')` 直接生成 | 改用 `CodeExtend::uniqidDate(16, 'P')` |
| `findOrEmpty()` 判断失败 | 用 `empty()` 判断对象 | 改用 `->isExists()` |
| 模型 append 字段在子查询中异常 | append 访问器依赖的字段未 select | 在 index 查询中通过 leftJoin 子查询实现 |
| `Phinx 迁移 SQL 语法错误` | `timestamp` 写了 `'update' => true` 或 `datetime` 写了 `'default' => null` | 按上文正确示例修正字段配置 |

---

## 六、多语言文件格式

语言文件位于 `app/{module}/lang/en-us.php`，采用以下格式：

```php
<?php

declare(strict_types=1);

$extra = [];
// 带变量的提示信息（可选）
$extra['中文原文'] = 'English translation with %s placeholder';
$extra['另一个提示'] = 'Another tip';

// 普通键值对
return array_merge($extra, [
    '中文键' => 'English Value',
    '搜索'   => 'Search',
    '删除'   => 'Delete',
]);
```

> 注意：lang 文件中的键名使用**中文**作为 key（而不是英文 key），模板中 `lang('搜索')` 传入中文键名，返回对应的英文翻译。
